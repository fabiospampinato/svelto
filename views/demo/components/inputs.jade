//- components/inputs.jade

extends ../../layout.jade

append body
    +divider Normal

    +multiple
        input(type="text", name="test", value="Base")
        input(type="text", name="test", value="Outlined").outlined
        input(type="text", name="test", value="Disabled" disabled="disabled")

    +divider Fluid

    input(type="text", name="test", value="Fluid").fluid

    +divider Toolbar

    +multiple
        +multiple.joined
            input(type="text", name="test", value="Search")
            +button.compact
                +icon("action-search")
        +multiple.joined
            input(type="text", name="test", value="Search")
            +button.compact.secondary
                +icon("action-search")
        +multiple.joined
            input(type="text", name="test", placeholder="Search...")
            +button.primary Search
        +multiple.joined
            +label.gray http://
            input(type="text", name="test", placeholder="Search...")
        +multiple.joined
            +label.compact
                +icon("social-people")
            input(type="text", name="test", value="Search")
        +multiple.joined
            input(type="text", name="test", value="Search")
            +label.compact
                +icon("navigation-refresh").rotating
        +multiple.joined
            +button.teal
                +button-left
                    +icon("maps-local-grocery-store")
                +button-center Checkout
            input(type="text", name="test", value="34.50€")

    +divider Toolbar - Outlined

    +multiple
        +multiple.joined.outlined
            input(type="text", name="test", value="Outlined")
            +button.compact
                +icon("action-search")
        +multiple.joined
            +label.gray http://
            input(type="text", name="test", placeholder="Outlined..." class="outlined")
