//- tagbox.jade

mixin tagbox(name, tags)
    +container&attributes(attributes).tagbox
        input(type="text", name=name, value=tags)
        +multiple
            input(type="text", name=("placeholder_" + name), value="", placeholder="New tag...").autogrow.compact.outlined
