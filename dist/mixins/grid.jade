//- grid.jade

mixin grid
    .grid&attributes(attributes)
        block

mixin grid-row
    .row&attributes(attributes)
        block

mixin grid-column
    .column&attributes(attributes)
        block
