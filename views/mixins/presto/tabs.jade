//- tabs.jade

mixin tabs
    .tabs&attributes(attributes)
        block

mixin tabs-buttons
    .tabs-buttons
        +multiple&attributes(attributes).fluid.joined
            block

mixin tabs-indicator
    .tabs-indicator&attributes(attributes)

mixin tabs-containers
    .tabs-containers&attributes(attributes)
        block
