//- radio.jade

mixin radio(name, title, value, checked)
    if checked
        .button.actionable.radio.checked&attributes(attributes)
            input(type="radio", name=name, value=value, checked)
            .button-center= title
                +icon("toggle-radio-button-on").on.right
                +icon("toggle-radio-button-off").off.right
    else
        .button.actionable.radio&attributes(attributes)
            input(type="radio", name=name, value=value)
            .button-center= title
                +icon("toggle-radio-button-on").on.right
                +icon("toggle-radio-button-off").off.right
