
extends ../layout.jade

//- Spinner

append main
  +divider-block Normal

  +container.outlined
    +multiple
      +spinner.primary
      +spinner.secondary
      +spinner.tertiary
      +spinner.quaternary
      +spinner.white
      +spinner.gray
      +spinner.black
      +spinner.yellow
      +spinner.olive
      +spinner.green
      +spinner.blue
      +spinner.violet
      +spinner.orange
      +spinner.purple
      +spinner.red
      +spinner.pink
      +spinner.teal
      +spinner.brown
      +spinner.error
      +spinner.warning
      +spinner.success
      +spinner.transparent
      +spinner.inherit
      +spinner.base

  +divider-block Label

  +multiple
    +spinner-label
    +spinner-label.white
    +spinner-label.white.outlined
    +spinner-label.primary
    +spinner-label.secondary
    +spinner-label.tertiary
    +spinner-label.quaternary

  +divider-block Multicolor

  +container.outlined
    +spinner.multicolor

  +divider-block Sizes

  +container.outlined
    +multiple
      +spinner.xxxxsmall
      +spinner.xxxsmall
      +spinner.xxsmall
      +spinner.xsmall
      +spinner.small
      +spinner.medium
      +spinner.large
      +spinner.xlarge
      +spinner.xxlarge
      +spinner.xxxlarge
      +spinner.xxxxlarge
