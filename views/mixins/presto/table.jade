//- table.jade

mixin table-tr-empty ( empty_str )
    tr.empty.hidden&attributes(attributes)
        td(colspan="100%")
            if block
                block
            else
                if empty_str
                    = empty_str
                else
                    = "Empty"
