//- shapes.jade

mixin triangle
    .triangle&attributes(attributes)
        block

mixin triangle-up
    .triangle-up&attributes(attributes)
        block

mixin triangle-down
    .triangle-down&attributes(attributes)
        block

mixin triangle-right
    .triangle-right&attributes(attributes)
        block

mixin triangle-left
    .triangle-left&attributes(attributes)
        block

mixin triangle-up-right
    .triangle-up-right&attributes(attributes)
        block

mixin triangle-up-left
    .triangle-up-left&attributes(attributes)
        block

mixin triangle-down-right
    .triangle-down-right&attributes(attributes)
        block

mixin triangle-down-left
    .triangle-down-left&attributes(attributes)
        block

mixin triangle-inset-up
    .triangle-inset-up&attributes(attributes)
        block

mixin triangle-inset-down
    .triangle-inset-down&attributes(attributes)
        block

mixin triangle-inset-right
    .triangle-inset-right&attributes(attributes)
        block

mixin triangle-inset-left
    .triangle-inset-left&attributes(attributes)
        block

mixin square
    .square&attributes(attributes)
        block

mixin square-sample
    .square-sample&attributes(attributes)
        p.no-margin Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et.
        block

mixin circle
    .circle&attributes(attributes)
        block
